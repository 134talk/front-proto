import { styled } from 'styled-components';

export const Container = styled.div`
  > section {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.2rem;
    margin-top: 7.3rem;
    p {
      font-size: 0.9rem;
    }
    img {
      width: 1.2rem;
      height: 1.2rem;
    }
  }
`;
