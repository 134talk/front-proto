import { styled } from 'styled-components';

export const Container = styled.div`
  width: 100%;
  .wrapper {
    width: 100%;
    margin-top: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
`;
