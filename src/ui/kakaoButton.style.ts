import { styled } from 'styled-components';

export const Container = styled.button`
  width: 100%;
  height: 2.8rem;
  background: #fee500;
  border-radius: 6px;
  position: relative;
  font-size: 0.94rem;
  font-weight: 500;
  color: rgba(0, 0, 0, 0.85);
  margin-top: 12rem;
  img {
    position: absolute;
    top: 0.875rem;
    left: 0.896rem;
  }
`;
