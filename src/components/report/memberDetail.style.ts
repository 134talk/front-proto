import { styled } from 'styled-components';

export const Container = styled.div`
  .wrapper {
    width: 100%;
    margin-top: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
`;
